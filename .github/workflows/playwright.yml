name: Playwright E2E Tests

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  # 1️⃣ Run tests matrix
  test:
    name: Playwright Tests
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          # Ubuntu
          - os: ubuntu-latest
            browser: chromium
          - os: ubuntu-latest
            browser: firefox
          # Windows
          - os: windows-latest
            browser: chromium
          - os: windows-latest
            browser: firefox
          - os: windows-latest
            browser: edge
          # macOS
          - os: macos-latest
            browser: chromium
          - os: macos-latest
            browser: webkit
          - os: macos-latest
            browser: firefox

    env:
      TEST_USER: ${{ secrets.TEST_USER }}
      TEST_PASSWORD: ${{ secrets.TEST_PASSWORD }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: lts/*

      - name: Install dependencies
        run: npm ci

      - name: Install Playwright Browsers
        run: npx playwright install --with-deps

      - name: Run Playwright tests
        shell: bash
        run: |
          if [[ "${{ matrix.os }}" == "ubuntu-latest" ]]; then
            xvfb-run npx playwright test --project=${{ matrix.browser }} --reporter=html,junit,json || true
          else
            npx playwright test --project=${{ matrix.browser }} --reporter=html,junit,json || true
          fi

      - name: Upload test report artifact
        uses: actions/upload-artifact@v4
        with:
          name: report-${{ matrix.os }}-${{ matrix.browser }}
          path: |
            playwright-report/
            jsonReports/jsonReport.json
            test-results.xml
          retention-days: 30
          if-no-files-found: warn

  # 2️⃣ Publish all reports
  publish:
    name: Publish Playwright Reports to GitHub Pages
    runs-on: ubuntu-latest
    needs: test
    # Run even if tests failed, but only on main branch
    if: always() && github.ref == 'refs/heads/main'

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Download all test artifacts
        uses: actions/download-artifact@v4
        with:
          path: ./reports

      - name: Generate index.html
        run: |
          echo "<!DOCTYPE html>
          <html lang='en'>
          <head><meta charset='UTF-8'><title>Playwright Reports</title></head>
          <body><h1>Playwright Reports</h1><ul>" > ./reports/index.html
          for dir in ./reports/*/; do
            report_name=$(basename $dir)
            echo "<li><a href='$report_name/index.html'>$report_name</a></li>" >> ./reports/index.html
          done
          echo "</ul></body></html>" >> ./reports/index.html

      - name: Publish to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.MY_PAT }}
          publish_dir: ./reports
          commit_message: "Publish Playwright reports [skip ci]"
